# 网络抓包分析工具依赖
# 安装命令: pip install -r requirements.txt

# PyQt5 - 图形界面框架
PyQt5>=5.15.0

# Scapy - 网络数据包操作库
scapy>=2.5.0

# 可选：libpcap 相关依赖（在 macOS 上可能需要）
# 在 macOS 上，建议使用 Homebrew 安装 libpcap:
# brew install libpcap
