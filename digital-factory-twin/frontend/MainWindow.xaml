<Window x:Class="DigitalFactoryTwin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DigitalFactoryTwin"
        xmlns:views="clr-namespace:DigitalFactoryTwin.Views"
        mc:Ignorable="d"
        Title="数字化工厂孪生系统 - Digital Factory Twin"
        Height="900" Width="1600"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="RadioButton" x:Key="NavigationButtonStyle">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#666666"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Foreground" Value="#2196F3"/>
                    <Setter Property="Background" Value="#E3F2FD"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Foreground" Value="#2196F3"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <Style TargetType="Button" x:Key="ActionButtonStyle">
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"
                                            TextBlock.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Border Grid.Row="0" Background="#2C3E50" Padding="20,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🏭" FontSize="28" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <StackPanel>
                        <TextBlock Text="数字化工厂孪生系统" 
                                   FontSize="20" 
                                   FontWeight="Bold" 
                                   Foreground="White"
                                   VerticalAlignment="Center"/>
                        <TextBlock Text="Digital Factory Twin System" 
                                   FontSize="10" 
                                   Foreground="#95A5A6"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Border Background="#1ABC9C" CornerRadius="4" Padding="10,5" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="●" FontSize="10" Foreground="White" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            <TextBlock Text="系统在线" FontSize="12" Foreground="White" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="{Binding CurrentTime, StringFormat='yyyy-MM-dd HH:mm:ss'}" 
                               FontSize="14" 
                               Foreground="White"
                               VerticalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Border Grid.Column="0" Background="White" BorderBrush="#E0E0E0" BorderThickness="0,0,1,0">
                <StackPanel>
                    <Border Background="#F8F9FA" Padding="15,10">
                        <TextBlock Text="导航菜单" FontSize="12" Foreground="#999999" FontWeight="Bold"/>
                    </Border>
                    
                    <RadioButton Content="🏠 系统概览" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 IsChecked="True"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="Overview"/>
                    
                    <RadioButton Content="🏭 产线3D监控" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="ProductionLine"/>
                    
                    <RadioButton Content="📊 APS排程优化" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="ApsOptimizer"/>
                    
                    <RadioButton Content="⚙️ 设备管理" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="DeviceManagement"/>
                    
                    <RadioButton Content="🔔 告警中心" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="Alerts"/>
                    
                    <Border Height="1" Background="#E0E0E0" Margin="10,20"/>
                    
                    <RadioButton Content="⚙️ 系统设置" 
                                 Style="{StaticResource NavigationButtonStyle}"
                                 Command="{Binding NavigateCommand}"
                                 CommandParameter="Settings"/>
                </StackPanel>
            </Border>
            
            <Grid Grid.Column="1" Margin="0">
                <ContentControl Content="{Binding CurrentView}"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
