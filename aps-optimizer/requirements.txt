# APS 排程优化器依赖

# 核心算法依赖
numpy>=1.24.0
deap>=1.4.1

# Web 服务依赖
aiohttp>=3.9.0

# 数据处理依赖
pandas>=2.0.0
python-dateutil>=2.8.2

# 可视化依赖 (可选，用于生成静态图表)
# plotly>=5.18.0
# matplotlib>=3.8.0

# 数据库依赖 (可选，用于持久化)
# sqlalchemy>=2.0.0

# 测试依赖 (开发环境)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
