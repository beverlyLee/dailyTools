# 网络分析与媒体转换工具依赖
# 安装命令: pip install -r requirements.txt

# PyQt5 - 图形界面框架
PyQt5>=5.15.0

# Scapy - 网络数据包操作库
scapy>=2.5.0

# MoviePy - 视频处理库（用于视频转GIF/WebM）
moviepy>=1.0.3

# Pillow - 图像处理库（用于GIF预览）
Pillow>=9.0.0

# NumPy - 数值计算库
numpy>=1.21.0

# 可选：libpcap 相关依赖（在 macOS 上可能需要）
# 在 macOS 上，建议使用 Homebrew 安装 libpcap:
# brew install libpcap

# 可选：ffmpeg（MoviePy 会自动下载，但建议系统安装）
# 在 macOS 上: brew install ffmpeg
# 在 Ubuntu 上: sudo apt install ffmpeg
